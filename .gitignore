/target
/result
/result-*
# Release artifacts from scripts/build-dist.sh.
/dist
.direnv/

# Feed and page captures taken by hand against a live site, for a replay
# directory or a bug report. Somebody else's content, never checked in.
/captures

# Editor and desktop cruft, so a contributor's tools do not land in a PR.
.DS_Store
.vscode/
.idea/
*.swp
*~

# The local starkit checkout override, documented in AGENTS.md. Never committed.
/.cargo/config.toml
